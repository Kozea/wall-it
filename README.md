# wall-it

## Dependencies :

### Flask :
pip install flask

### Google connection :
pip install httplib2
pip install oauth2client

### Pygal :
pip install pygal

## Database init :

sqlite3 /tmp/wallit.db < schema.sql
